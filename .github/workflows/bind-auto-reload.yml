name: Update BIND Zones

on:
  push:
    branches:
      - main

jobs:
  update-dns:
    runs-on: self-hosted  # Your runner on ns1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run DNS Serial Update Script
         #run: |
        #   sudo -n /bin/bash /etc/bind/scripts/update_serial.sh
        run: echo "lava*001" | sudo -S /bin/bash /etc/bind/scripts/update_serial.sh
